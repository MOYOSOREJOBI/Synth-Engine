{
  "name": "@pulsesynth/wasm-dsp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release --target wasm32-unknown-unknown && node scripts/copy-wasm.js",
    "build:native": "cargo build --release",
    "test": "cargo test -- --test-threads=1"
  }
}
